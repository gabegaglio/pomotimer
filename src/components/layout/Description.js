import React from 'react';

const Description = ({  }) => {
  
 const work = "Let's get to work!";
 const rest = "Take a break, you've deserved it!";

  return (
    <div className="desc text-white text-opacity-90 text-lg mt-4">
      {work}
    </div>
  );
};

export default Description;
